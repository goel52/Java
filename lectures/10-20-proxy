DYNAMIC PROXY
==============

В runtime создается объект, который может притворяться любым классом.
Декоратор имеет ссылку на класс, который он декорирует. Каждый свой метод он делегирует этот же метод этой ссылке + добавляет какую-то логику.

Прокси похож на декоратор, но не привязан к конкретному типу.
Написали хэширующий и логирующий прокси. 

Аннотации интерфейся и делегатов.

Методы аннотацийй.




